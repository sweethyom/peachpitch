version: '3.8'

services:
  react:
    build: .
    container_name: my-react
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=development
    network:
      - peachpitch-network

networks:
  peachpitch-network:
    external: true